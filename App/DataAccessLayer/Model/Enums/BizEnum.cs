using System;
using Intersoft.CISSA.DataAccessLayer.Repository;

namespace Intersoft.CISSA.DataAccessLayer.Model.Enums
{
    /*[Obsolete("Не используется!")]
    public static class BizEnum
    {
        public static Guid Value(string enumName, string enumValue)
        {
            using (var repo = new EnumRepository())
            {
                Guid enumId = repo.GetEnumDefId(enumName);
                return repo.GetEnumValueId(enumId, enumValue);
            }
        }
    }*/
}
