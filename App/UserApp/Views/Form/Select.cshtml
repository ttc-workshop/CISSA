@model Intersoft.CISSA.UserApp.Models.ManagedTableForm
@{
    var caption = ViewData["FormCaption"];
    ViewBag.Title = caption != null ? caption.ToString() : "";
}
@if (Model != null)
{
    ViewData["Selection"] = true;
    @Html.Partial("_" + Model.GetType().Name, Model);
}